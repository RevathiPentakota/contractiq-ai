"""ORM models package.

SQLAlchemy ORM model classes live here.  All models must inherit from
``app.db.base.Base``.  Register each model module in ``app/db/base.py``
so Alembic can detect schema changes automatically.
"""
